package com.beet.backend.modules.${moduleName}.domain.spi;

import java.util.UUID;

public interface ${Target}Gateway{
// Define methods needed from external modules
// Example: int getMaxLimit(UUID userId);
// Return primitives or local DTOs, avoid returning external Domain objects
// directly.
}
